# Synthetic data

Every file here is fictional and generated solely for the Harbor FCU textbook. `api_requests.csv` is a deliberately small initial fixture of 20 synthetic balance-API observations. It contains no member identifiers or real financial data. Later generators will use deterministic seeds and document their schemas here.
