# Part VII synthetic telemetry provenance

Part VII derives deterministic observations in `src/harbor_fcu/intelligence.py`. They reuse the fictional API vendors, request latency and failures, deployment state, database latency, and incident concepts established in Parts III–VI. Counts and known incident labels are deliberately authored ground truth, contain no member records, and require no network or AI service. Change the scenario functions and their tests together so reported results remain reproducible.
